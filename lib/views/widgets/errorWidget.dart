import 'package:flutter/material.dart';

class renderError extends StatelessWidget {
  const renderError({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Error Fetching Data"));
  }
}
